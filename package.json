{
  "name": "illustconverter",
  "version": "1.0.0",
  "description": "IllustConverter - NovelAI Inpaint Service",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && node src/index.js",
    "dev": "echo 'Run server and client separately: cd server && npm run dev / cd client && npm run dev'"
  }
}
